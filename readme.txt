Conocimientos aprendidos con este ejercicio.

Crear y organizar los archivos .js.
Importar archivos .js, siempre al final del <body> en el HTML.
La representación del HTML por el objeto document.
La función querySelector.
Saber escoger entre usar #id o .clase.

Breve revisión de lógica de programación.
Condicionales if.
Operadores matemáticos.
Operadores lógicos: O ( | | ), Y ( && ).

Cómo seleccionar diversos elementos con querySelectorAll().
Recordamos las iteraciones con ciclo for.
Manipulación de estilos con la propiedad. style.
Buenas prácticas al no alterar el estilo directo en JavaScript, sino usar CSS, a través de clases con la propiedad .classList().

La diferencia entre funciones declaradas y funciones anónimas.
Realizar captura de eventos con la función addEventListener().
Crear elementos HTML desde JavaScript con .createElement().
Acceder al valor de los input a través de la propiedad .input.
Adicionar elementos dentro de otros elementos con la función appendChild().

Dividir nuestro código en archivos diferentes con responsabilidades definidas.
A quebrar funciones grandes en funciones más pequeñas.

A dividir nuestro código, separándolo en funciones con responsabilidades menores.
A crear objetos en JavaScript.
La función form.reset() para limpiar el formulario.

Más buenas prácticas, exportando las funciones de validación.
El operador de negación NOT (!).
Cómo validar un formulario.
La función Push para colocar elementos dentro de un array.
La propiedad innerHTML de los elementos, usada para eliminar los ítems de la tabla.
El método forEach para iterar los arrays.

Un nuevo evento: dblclick.
La característica Event Bubbling (propagación de eventos).
La delegación de eventos.
Cómo animar una remoción con el setTimeout.

Un nuevo evento: input.
El truco de dejar invisible un elemento adicionando una clase con display: none.
Cómo realizar una búsqueda, escondiendo todos los elementos y mostrando apenas aquellos que nos interesan.
A usar las Expresiones Regulares para realizar búsquedas sofisticadas que nos permitan realizar filtros.

Cómo hacer una requisición AJAX asíncrona con JavaScript.
El objeto XMLHttpRequest.
Cómo configurar y enviar una requisición.
El evento load.
Archivos de formato JSON y cómo convertirlos para un objeto de JavaScript.
Cómo lidiar con errores durante las requisiciones AJAX.